name: Build

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 25

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Build JAR
        run: ./gradlew clean jar --no-daemon --stacktrace --info

      - name: Check JAR
        if: always()
        run: |
          echo "=== BUILD DIRECTORY ==="
          ls -la build || true
          echo "=== BUILD/LIBS ==="
          ls -la build/libs || true
          echo "=== ALL JARS ==="
          find . -type f -name "*.jar" -print

      - name: Upload JAR
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: void-stalker-jar
          path: build/libs/*.jar
          if-no-files-found: warn
